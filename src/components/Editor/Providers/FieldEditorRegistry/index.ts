import {
  useFieldEditorRegistry,
  useFieldEditorRegistryState,
  useFieldEditorRegistryDispatch,
} from './hooks';

import FieldEditorRegistryProvider from './Provider';

export default {
  FieldEditorRegistryProvider,

  useFieldEditorRegistry,
  useFieldEditorRegistryDispatch,
  useFieldEditorRegistryState,
}